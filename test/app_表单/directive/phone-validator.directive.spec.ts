import { PhoneValidatorDirective } from './mobile-validator.directive';

describe('MobileValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
